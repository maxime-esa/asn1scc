dependencies:
  pre:
    - echo 'deb http://httpredir.debian.org/debian stretch main' | sudo tee -a /etc/apt/sources.list
    - sudo apt-get update
    - sudo apt-get install mono-devel mono-complete fsharp mono-xbuild python3 gnat-6

test:
  override:
      - ./circleci-build.sh
