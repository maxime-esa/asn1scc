machine:
  environment:
    USE_SYSTEM_NUNIT_CONSOLE: 1

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install mono-devel mono-complete fsharp mono-xbuild

test:
  override:
      - ./circleci-build.sh
